import LoginClient from "@/components/LoginClient";


export const metadata = {
  title: "Login - MediQueue",
  description: "Login to your account to manage your personalized learning and tutor sessions.",
};

const LoginPage = () => {
  return <LoginClient />;
};

export default LoginPage;